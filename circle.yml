machine:
  services:
    - docker

test:
  override:
    - docker-compose run web npm run cover
  post:
    - docker-compose run web cat test-results.xml | $CIRCLE_TEST_REPORTS/junit/test-results.xml
